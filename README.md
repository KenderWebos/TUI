pip install textual

python app.py

Textual      → interfaz principal
Rich         → tablas, logs, estilos
Typer        → comandos CLI
uv           → entorno y dependencias
pytest       → tests
ruff         → formato/lint
sqlite       → persistencia local
pydantic     → config validada